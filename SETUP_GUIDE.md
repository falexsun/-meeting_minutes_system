# Руководство по настройке и исправлению ошибок

## ✅ Уже сделано:

1. ✅ Проект установлен в виртуальное окружение
2. ✅ Все зависимости установлены
3. ✅ Файл `.env` настроен

## 🔧 Что нужно сделать:

### 1. Запустить Ollama

**Проверка, запущен ли Ollama:**
```powershell
curl http://127.0.0.1:11434/api/tags
```

Если получаете ошибку "Невозможно соединиться":

**Вариант A: Запустить Ollama (если уже установлен)**
- Найдите "Ollama" в меню Пуск и запустите
- Или посмотрите в трее (правый нижний угол) - там должна быть иконка ламы

**Вариант B: Установить Ollama (если не установлен)**
1. Скачайте с https://ollama.com/download (Windows)
2. Запустите установщик
3. Ollama автоматически запустится

### 2. Загрузить модель qwen2.5:7b

**После запуска Ollama выполните:**
```powershell
ollama pull qwen2.5:7b
```

Это загрузит модель (~4.7 GB). Подождите завершения.

**Проверить, что модель загружена:**
```powershell
ollama list
```

### 3. Запустить Meminto

**Активируйте виртуальное окружение и запустите:**

```powershell
# Вариант 1: Использовать полный путь к Python из venv
C:/code/tsom/protocolirovanie/Meminto/venv/Scripts/python.exe meminto/main.py -f путь/к/аудио.wav -l russian --use-ollama

# Вариант 2: Активировать venv и запустить
cd c:\code\tsom\protocolirovanie\Meminto
.\venv\Scripts\Activate.ps1
python meminto/main.py -f путь/к/аудио.wav -l russian --use-ollama
```

**Пример с тестовым файлом из репозитория:**
```powershell
C:/code/tsom/protocolirovanie/Meminto/venv/Scripts/python.exe meminto/main.py -f examples/Scoreboard.wav -l english --use-ollama
```

## 📝 Типичные ошибки и их решения

### Ошибка: "ModuleNotFoundError: No module named 'meminto'"

**Решение:** Используйте Python из виртуального окружения:
```powershell
C:/code/tsom/protocolirovanie/Meminto/venv/Scripts/python.exe meminto/main.py --help
```

### Ошибка: "HUGGING_FACE_ACCESS_TOKEN not found"

**Решение:** 
1. Убедитесь, что файл `.env` находится в корневой папке проекта
2. Проверьте, что в `.env` есть строка:
   ```
   HUGGING_FACE_ACCESS_TOKEN=ваш_токен
   ```
3. Получите токен на https://huggingface.co/settings/tokens

### Ошибка: "Connection refused" при подключении к Ollama

**Решение:**
1. Проверьте, что Ollama запущен (см. шаг 1)
2. Проверьте URL в `.env`:
   ```
   OLLAMA_URL=http://127.0.0.1:11434
   ```
3. Убедитесь, что порт 11434 не занят другим приложением

### Ошибка: "Model 'qwen2.5:7b' not found"

**Решение:**
```powershell
ollama pull qwen2.5:7b
```

## 🚀 Быстрый старт

**Если все настроено, запускайте так:**

```powershell
cd c:\code\tsom\protocolirovanie\Meminto

# 1. Активировать виртуальное окружение
.\venv\Scripts\Activate.ps1

# 2. Запустить обработку аудио
python meminto/main.py -f your_audio.wav -l russian --use-ollama
```

**Результаты будут в папке `output/`:**
- `diarization.txt` - кто и когда говорил
- `transcript.txt` - полная расшифровка
- `meeting_minutes.txt` - протокол совещания

## 💡 Полезные команды

**Проверить, что все работает:**
```powershell
# 1. Проверить Python
C:/code/tsom/protocolirovanie/Meminto/venv/Scripts/python.exe --version

# 2. Проверить meminto
C:/code/tsom/protocolirovanie/Meminto/venv/Scripts/python.exe meminto/main.py --help

# 3. Проверить Ollama
curl http://127.0.0.1:11434/api/tags

# 4. Список моделей Ollama
ollama list
```

**Если нужна помощь:**
- Проверьте логи в `output/time_log.txt`
- Запустите с тестовым файлом: `examples/Scoreboard.wav`
