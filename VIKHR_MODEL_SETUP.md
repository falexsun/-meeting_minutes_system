# Настройка модели Vikhr-Nemo для LM Studio

## О модели vikhr-nemo-12b-instruct-r-21-09-24

**Vikhr-Nemo** - это большая языковая модель, оптимизированная для работы с русским языком. Она основана на архитектуре Nemo и содержит 12 миллиардов параметров.

### Преимущества:
- ✅ Оптимизирована для русского языка
- ✅ 12B параметров - отличный баланс качества и производительности
- ✅ Превосходное понимание контекста совещаний
- ✅ Точная генерация формальных протоколов

## Загрузка модели в LM Studio

### Вариант 1: Поиск в LM Studio

1. Откройте LM Studio
2. Перейдите на вкладку **🔍 Search** (Поиск)
3. В строке поиска введите: `vikhr-nemo-12b-instruct`
4. Найдите модель **vikhr-nemo-12b-instruct-r-21-09-24**
5. Выберите квантизацию:
   - **Q4_K_M** - рекомендуется для большинства компьютеров (~7GB RAM)
   - **Q5_K_M** - лучшее качество, требует больше RAM (~8.5GB RAM)
   - **Q6_K** - максимальное качество (~10GB RAM)
6. Нажмите **Download** (Загрузить)

### Вариант 2: Ручная загрузка с Hugging Face

Если модель не найдена в LM Studio:

1. Перейдите на Hugging Face: https://huggingface.co/
2. Найдите модель: `vikhr-nemo-12b-instruct-r-21-09-24-GGUF`
3. Скачайте файл `.gguf` с нужной квантизацией
4. В LM Studio:
   - Перейдите в **⚙️ Settings** → **Model Folder**
   - Скопируйте скачанный файл в папку моделей
   - Модель появится в списке доступных моделей

## Запуск модели

1. В LM Studio перейдите на вкладку **💬 Chat** или **🔌 Local Server**
2. Выберите **vikhr-nemo-12b-instruct-r-21-09-24** из списка моделей
3. Настройте параметры:
   ```
   Context Length: 8192 (рекомендуется для длинных протоколов)
   GPU Offload: Maximum (если есть GPU)
   Temperature: 0.5 (уже настроено в коде)
   ```
4. Нажмите **Start Server** (если используете Local Server)
5. Убедитесь, что сервер запущен на `http://localhost:1234`

## Настройка .env файла

Создайте файл `.env` из `.env.example`:

```powershell
Copy-Item .env.example .env
```

Отредактируйте `.env`:

```env
HUGGING_FACE_ACCESS_TOKEN=ваш_токен_здесь

LMSTUDIO_MODEL=vikhr-nemo-12b-instruct-r-21-09-24
LMSTUDIO_URL=http://localhost:1234/v1/chat/completions
LMSTUDIO_MAX_TOKENS=8000
```

## Запуск программы

```powershell
# Активируйте виртуальное окружение
.\venv\Scripts\Activate.ps1

# Запустите обработку
python meminto/main.py -f examples/Scoreboard.wav -l russian
```

## Системные требования

### Минимальные (Q4_K_M):
- RAM: 16GB (8GB для модели + 8GB для системы)
- CPU: 4+ ядра
- Диск: 10GB свободного места

### Рекомендуемые (Q5_K_M):
- RAM: 24GB (12GB для модели + 12GB для системы)
- CPU: 6+ ядер
- GPU: NVIDIA с 8GB+ VRAM (опционально, ускорит обработку в 3-5 раз)
- Диск: 15GB свободного места

### Оптимальные (Q6_K с GPU):
- RAM: 32GB
- CPU: 8+ ядер
- GPU: NVIDIA RTX 3060/4060 или лучше (12GB+ VRAM)
- Диск: 20GB свободного места

## Проверка работоспособности

### Тест в LM Studio Chat

1. Откройте вкладку **💬 Chat**
2. Выберите модель **vikhr-nemo-12b-instruct-r-21-09-24**
3. Отправьте тестовый запрос:
   ```
   Создай краткий протокол совещания на тему "Планирование квартала"
   ```
4. Если модель отвечает на русском языке - всё работает!

### Тест через API

```powershell
# Тест через curl
curl http://localhost:1234/v1/chat/completions `
  -H "Content-Type: application/json" `
  -d '{
    "model": "vikhr-nemo-12b-instruct-r-21-09-24",
    "messages": [
      {"role": "system", "content": "Ты - помощник для создания протоколов."},
      {"role": "user", "content": "Привет! Как дела?"}
    ],
    "temperature": 0.5,
    "max_tokens": 100
  }'
```

## Устранение проблем

### Ошибка: "Model not found"
- Убедитесь, что модель загружена в LM Studio
- Проверьте точное название модели в `.env`
- Перезапустите LM Studio

### Ошибка: "Out of memory"
- Выберите меньшую квантизацию (Q4_K_M вместо Q5_K_M)
- Уменьшите `Context Length` до 4096
- Закройте другие программы

### Медленная генерация
- Включите GPU Offload в настройках LM Studio
- Уменьшите `LMSTUDIO_MAX_TOKENS` до 4000-6000
- Используйте меньшую квантизацию для ускорения

### Низкое качество протоколов
- Увеличьте квантизацию до Q5_K_M или Q6_K
- Увеличьте `Context Length` до 8192
- Проверьте, что модель именно vikhr-nemo (оптимизирована для русского)

## Альтернативные модели

Если vikhr-nemo не подходит, попробуйте:

1. **Qwen/Qwen2.5-7B-Instruct** (меньше, быстрее)
2. **Llama-3.1-8B-Instruct** (универсальная)
3. **saiga_llama3_8b** (русскоязычная)

Измените в `.env`:
```env
LMSTUDIO_MODEL=название_другой_модели
```

## Контакты и поддержка

Если возникли проблемы:
1. Проверьте логи в консоли
2. Убедитесь, что LM Studio запущен
3. Проверьте системные требования
4. Изучите документацию LM Studio: https://lmstudio.ai/docs
