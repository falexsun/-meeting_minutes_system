# 🚀 Быстрый старт Meminto (Python 3.11)

## ✅ Текущая конфигурация

- Python: **3.11.9** (`C:\Users\alexf\AppData\Local\Programs\Python\Python311\python.exe`)
- Виртуальное окружение: `venv/`
- Ollama: работает на `http://127.0.0.1:11434`
- Модель: `qwen2.5:7b`

## 📦 Установка зависимостей (если еще не установлены)

```powershell
cd c:\code\tsom\protocolirovanie\Meminto

# Активировать виртуальное окружение
.\venv\Scripts\Activate.ps1

# Установить все зависимости
pip install -e .
```

## 🎯 Запуск программы

### Базовый запуск

```powershell
# Активировать окружение (если еще не активировано)
.\venv\Scripts\Activate.ps1

# Запустить обработку аудиофайла
python meminto/main.py -f путь\к\audio.wav -l russian --use-ollama
```

### Примеры команд

```powershell
# Обработать тестовый файл на английском
python meminto/main.py -f examples/Scoreboard.wav -l english --use-ollama

# Обработать свой файл на русском с указанием папки вывода
python meminto/main.py -f C:\путь\к\meeting.wav -o output -l russian --use-ollama

# Использовать удаленный транскрайбер + Ollama
python meminto/main.py -f meeting.wav --remote-transcriber --use-ollama -l russian
```

### Без активации виртуального окружения

Если не хотите активировать venv каждый раз:

```powershell
.\venv\Scripts\python.exe meminto/main.py -f audio.wav -l russian --use-ollama
```

## 🔧 Проверка системы

### 1. Проверить Python

```powershell
.\venv\Scripts\python.exe --version
# Должно быть: Python 3.11.9
```

### 2. Проверить Ollama

```powershell
curl http://127.0.0.1:11434/api/tags
# Должно показать список моделей, включая qwen2.5:7b
```

### 3. Проверить установленные пакеты

```powershell
.\venv\Scripts\pip.exe list | Select-String "torch|click|pyannote"
```

### 4. Тест импорта

```powershell
.\venv\Scripts\python.exe -c "import torch, click, pyannote.audio; print('✅ Все ОК!')"
```

## 📂 Структура выходных файлов

После обработки в папке `output/` будут созданы:

```
output/
├── diarization.txt       # Кто и когда говорил
├── diarization.pkl       # Данные диаризации (промежуточный файл)
├── transcript.txt        # Полная расшифровка с временными метками
├── transcript.pkl        # Данные транскрипции (промежуточный файл)
├── meeting_minutes.txt   # Финальный протокол совещания
└── time_log.txt         # Логи выполнения
```

## ⚙️ Переменные окружения (.env)

Убедитесь, что файл `.env` содержит:

```bash
# Обязательно
HUGGING_FACE_ACCESS_TOKEN=ваш_токен

# Для Ollama (при использовании --use-ollama)
OLLAMA_MODEL=qwen2.5:7b
OLLAMA_URL=http://127.0.0.1:11434
OLLAMA_MAX_TOKENS=8000
```

## 🐛 Решение проблем

### Ошибка: "ModuleNotFoundError"

```powershell
# Переустановить зависимости
.\venv\Scripts\pip.exe install -e .
```

### Ollama не отвечает

```powershell
# Проверить статус
curl http://127.0.0.1:11434/api/tags

# Если не работает - перезапустить Ollama
# (найдите в трее и перезапустите)
```

### Slow performance / Out of memory

```bash
# В .env уменьшите max_tokens
OLLAMA_MAX_TOKENS=4000

# Или используйте модель поменьше
ollama pull qwen2.5:3b
OLLAMA_MODEL=qwen2.5:3b
```

## 📊 Время обработки (примерное)

- **5 минут аудио**: ~3-5 минут
- **30 минут аудио**: ~15-20 минут  
- **1 час аудио**: ~30-40 минут
- **2 часа аудио**: ~60-90 минут

*Зависит от мощности процессора и наличия GPU*

## 💡 Полезные советы

1. **Формат аудио**: лучше использовать WAV 16kHz моно
2. **Качество записи**: чем чище звук, тем лучше результат
3. **Длинные совещания**: для файлов >1 часа увеличьте `OLLAMA_MAX_TOKENS` до 8000
4. **GPU**: если есть NVIDIA GPU, PyTorch автоматически его использует

## 🆘 Получить помощь

1. Проверьте `output/time_log.txt` для деталей ошибки
2. Запустите с тестовым файлом для проверки работоспособности
3. Посмотрите `TROUBLESHOOTING.md` для решения частых проблем
