# Решение проблем при запуске Meminto

## ⚠️ Основная проблема: Python 3.13

У вас установлен Python 3.13.1, но проект требует **Python 3.11** согласно `pyproject.toml`.

Python 3.13 слишком новый, и многие научные библиотеки (PyTorch, Transformers, SymPy, matplotlib) еще не полностью совместимы с ним, что вызывает:
- Очень долгую инициализацию (зависания при импорте)
- Ошибки совместимости
- Проблемы с matplotlib

## ✅ Решение: Установить Python 3.11

### Вариант 1: Установка Python 3.11 (рекомендуется)

1. **Скачайте Python 3.11:**
   - Перейдите на https://www.python.org/downloads/
   - Скачайте Python 3.11.x (последняя версия 3.11)
   - При установке поставьте галочку "Add Python to PATH"

2. **Создайте новое виртуальное окружение:**
   ```powershell
   cd c:\code\tsom\protocolirovanie\Meminto
   
   # Удалите старое виртуальное окружение
   Remove-Item venv -Recurse -Force
   
   # Создайте новое с Python 3.11
   python3.11 -m venv venv
   
   # Или если Python 3.11 установлен как основной:
   py -3.11 -m venv venv
   ```

3. **Активируйте виртуальное окружение:**
   ```powershell
   .\venv\Scripts\Activate.ps1
   ```

4. **Установите зависимости:**
   ```powershell
   # Обновите pip
   python -m pip install --upgrade pip
   
   # Установите проект
   pip install -e .
   ```

5. **Запустите Meminto:**
   ```powershell
   python meminto/main.py -f examples/Scoreboard.wav -l english --use-ollama
   ```

### Вариант 2: Использовать Conda (альтернатива)

Если у вас есть Anaconda или Miniconda:

```powershell
# Создайте окружение с Python 3.11
conda create -n meminto python=3.11

# Активируйте окружение
conda activate meminto

# Установите зависимости
cd c:\code\tsom\protocolirovanie\Meminto
pip install -e .

# Запустите
python meminto/main.py -f examples/Scoreboard.wav -l english --use-ollama
```

## 🔧 Временное решение (с Python 3.13)

Если вы не можете сейчас установить Python 3.11, попробуйте:

### 1. Установить более старые версии библиотек

```powershell
cd c:\code\tsom\protocolirovanie\Meminto
.\venv\Scripts\Activate.ps1

# Установите совместимые версии
pip install torch==2.4.0 torchaudio==2.4.0
pip install "transformers<4.50"
pip install "matplotlib<3.9"
pip install "sympy<1.13"
```

### 2. Отключить JIT компиляцию

Перед запуском установите переменные окружения:

```powershell
$env:PYTORCH_JIT=0
$env:TORCH_COMPILE_DISABLE=1

python meminto/main.py -f examples/Scoreboard.wav -l english --use-ollama
```

### 3. Упростить код tokenizer.py

Если проблема в токенизаторе, можно временно отключить его использование для Ollama.

Откройте `meminto/main.py` и найдите строки:

```python
# Создаем упрощенный токенизатор (без доступа к HuggingFace)
# Для Ollama можно использовать приблизительный подсчет токенов
tokenizer = Tokenizer(
    model_name,
    hugging_face_acces_token=os.environ.get("HUGGING_FACE_ACCESS_TOKEN", ""),
)
```

Замените на:

```python
# Для Ollama используем None в качестве токенизатора
# Он не критичен для работы
tokenizer = None
```

Затем в `meminto/meeting_minutes_generator.py` добавьте проверку на None.

## 📝 Проверка текущей версии Python

```powershell
python --version
# Должно быть: Python 3.11.x

# Или проверьте все установленные версии:
py -0
```

## 🎯 Рекомендации

### Для продуктивной работы:
1. ✅ Установите Python 3.11
2. ✅ Создайте чистое виртуальное окружение
3. ✅ Установите все зависимости заново

### Для быстрого тестирования:
1. Используйте временное решение выше
2. Будьте готовы к долгой инициализации (1-2 минуты)

## 🚀 После переустановки с Python 3.11

Полная последовательность команд:

```powershell
# 1. Перейти в папку проекта
cd c:\code\tsom\protocolirovanie\Meminto

# 2. Удалить старое окружение
Remove-Item venv -Recurse -Force

# 3. Создать новое с Python 3.11
py -3.11 -m venv venv

# 4. Активировать
.\venv\Scripts\Activate.ps1

# 5. Обновить pip
python -m pip install --upgrade pip

# 6. Установить проект
pip install -e .

# 7. Проверить, что Ollama запущен
curl http://127.0.0.1:11434/api/tags

# 8. Запустить обработку
python meminto/main.py -f examples/Scoreboard.wav -l english --use-ollama
```

## 💡 Дополнительная информация

### Почему Python 3.11, а не 3.13?

- Python 3.13 вышел совсем недавно (октябрь 2024)
- Научные библиотеки (PyTorch, NumPy, SciPy) обычно отстают на 6-12 месяцев
- Python 3.11 стабилен и полностью поддерживается всеми библиотеками
- Python 3.11 также очень быстрый (значительное улучшение по сравнению с 3.10)

### Системные требования (обновленные):

- ✅ Python 3.11.x (обязательно!)
- ✅ 8-16 GB RAM
- ✅ Ollama с моделью qwen2.5:7b
- ✅ Hugging Face токен для диаризации
- 🎯 GPU опционально (ускорит транскрипцию)

## 📞 Если проблемы остались

1. Проверьте версию Python: `python --version`
2. Проверьте, что Ollama запущен: `curl http://127.0.0.1:11434/api/tags`
3. Проверьте `.env` файл: должен содержать `HUGGING_FACE_ACCESS_TOKEN`
4. Посмотрите логи в `output/time_log.txt`
5. Запустите с флагом `-v` для подробного вывода (если добавлен)

## ✅ Признаки успешного запуска

При корректном запуске вы должны увидеть:

```
Using LocalTranscriber.
Using Ollama for LLM.
Url используемый для LLM запроса: http://127.0.0.1:11434
Модель: qwen2.5:7b
Loading model: pyannote/speaker-diarization@2.1
Diarizing audio...
Transcribing audio...
Generating meeting minutes...
```

Процесс займет несколько минут в зависимости от длины аудио.
